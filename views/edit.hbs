<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <!-- CSS only -->
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
            crossorigin="anonymous">
        <style>
        table,th, td {
            border: 1px solid black;
            border-collapse: collapse;
        }
    </style>
        <title>Edit</title>
    </head>
    <body class="p-3 mb-2 bg-secondary text-white">
        <div class="container">
            <div class="menu">
                <nav class="navbar navbar-expand-lg navbar-light bg-light">
                    <div class="container-fluid">
                        <a><img
                                src="https://think.edu.vn/wp-content/uploads/2020/05/University-of-Greenwich.jpg"
                                width="100px" height="100px"></a>
                        <div class="collapse navbar-collapse"
                            id="navbarSupportedContent">
                            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                                <li class="nav-item">
                                    <a class="nav-link active"
                                        aria-current="page" href="/">Home</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </nav>
            </div>
            <br>
            <h1 style="text-align: center; color: red;" class="">Update Product</h1>
            <div style="width: 100%; text-align: center;">
                <form action="/update" method="post">
                    <input type="hidden" name="txtId" value="{{product._id}}">
                    <div class="mb-3">
                        <label class="form-label" for="txtName"> Product Name</label>
                        <input type="text" class="form-control" name="txtName"
                            id="txtName" value="{{product.name}}">
                    </div>
                    <div class="mb-3">
                        <label class="form-label" for="txtPrice"> Price</label>
                        <input type="number" class="form-control"
                            name="txtPrice" id="txtPrice"
                            value="{{product.price}}">
                    </div>
                    <div class="mb-3">
                        <label class="form-label" for="txtPrice"> Quantity</label>
                        <input type="number" class="form-control"
                            name="txtQuantity" id="txtQuantity"
                            value="{{product.quantity}}">
                    </div>
                    <div class="mb-3">
                        <label class="form-label" for="txtURL"> Picture</label>
                        <input type="text" class="form-control" name="txtURL"
                            value="{{product.url}}">
                    </div>
                    <input type="submit" class="btn btn-primary" value="Update">
                </form>
            </div>
            <hr>
            <div>
                <table class="table table-dark table-striped">
                    <tr>
                        {{!-- <th style="width:30%">id</th> --}}
                        <th style="width:40%">Name</th>
                        <th style="width:30%">Price</th>
                        <th style="width:30%">quantity</th>
                        <th style="width:60%">Picture</th>
                    </tr>
                    <tr>
                        {{!-- <td>{{product._id}}</td> --}}
                        <td>{{product.name}}</td>
                        <td>{{product.price}}</td>
                        <td>{{product.quantity}}</td>
                        <td><img src="{{product.picURL}}" width="300"
                                height="200" </td>
                    </tr>
                </table>
            </div>
        </div>
    </body>
</html>